{
  "name": "gothailand-yok",
  "private": true,
  "version": "0.0.0",
  "description": "Root orchestrator for the frontend/ (Vite+React) and backend/ (Express+MongoDB) packages. Run `npm run install:all` once, then `npm run dev:full`.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "server": "npm run server --prefix backend",
    "server:dev": "npm run server:dev --prefix backend",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "dev:full": "concurrently -n API,WEB -c blue,green \"npm:server:dev\" \"npm:dev\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
