PORT=5001
NODE_ENV=development

# MongoDB Atlas Connection URI (ชี้ไปที่ database gothailand_user)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/gothailand_user?retryWrites=true&w=majority

# JWT Secret Key
JWT_SECRET=gt_super_secret_jwt_key_2026
JWT_EXPIRES_IN=7d

# Allowed Frontend Origins for CORS (comma-separated or *)
CLIENT_URL=*
